

---

description :añade los cambios del repositorio al staging area ,haz commit con un mensaje claro y descriptivo que siga 'fase N description'
---

añade los cambios al staging area :

```bash
git add _A


```
haz el commit con un mensaje que siga el formato `fase N description`, donde :
- `N` es el numero de fase (1,2,3,etc.).
- `description` es un resumen breve de lo que has hecho en esta fase .
```bach
git commit -m "fase N descripcion "
```